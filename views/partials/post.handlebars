
<div class="card mb-3" style="max-width: 20rem;">
  <div class="card-header bg-primary text-white">{{post.user_id}} , {{post.created_on}} </div>
  <div class="card-body">
    <h4 class="card-title">{{post.title}}</h4>
    <p class="card-text"> {{post.content}}</p>
  </div>
</div>
